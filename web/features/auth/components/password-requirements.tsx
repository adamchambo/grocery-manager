export function PasswordRequirements() {
  return (
    <p className="text-xs leading-relaxed text-muted-foreground">
      Use 6–100 characters with uppercase, lowercase, number, and symbol.
    </p>
  )
}
